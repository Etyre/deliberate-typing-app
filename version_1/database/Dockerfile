FROM postgres:16.2
ARG POSTGRES_USER
ARG POSTGRES_PASSWORD
ARG PORT

ENV POSTGRES_USER=${POSTGRES_USER}

ENV POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

ENV POSTGRES_DB=deliberate_typing_app

ENV PGDATA=/var/lib/postgresql/data/deliberate_typing_app

EXPOSE ${PORT}